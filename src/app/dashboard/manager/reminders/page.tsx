import RemindersClient from "@/components/reminders/RemindersClient";

export const metadata = {
    title: "TKPI Management | Reminders",
};

export default function RemindersPage() {
    return <RemindersClient />;
}
